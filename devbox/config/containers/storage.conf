# Storage configuration for rootless Buildah/Podman
# This config is copied to ~/.config/containers/ at runtime

[storage]
driver = "vfs"
runroot = "/run/containers"
graphroot = "/home/minion/.local/share/containers/storage"

[storage.options]
# VFS driver doesn't require special options
# It's slower but works without any kernel features or privileges
