# Progress Log
PRD: minions-system
Started: 2026-03-21

## Codebase Patterns
- Migrations: `schema/migrations/NNN_description.sql` - raw SQL, no ORM
- UUID PKs with `gen_random_uuid()` default
- Timestamps: `TIMESTAMPTZ NOT NULL DEFAULT NOW()` for created_at/updated_at
- Go services: chi router, JSON handlers, slog for structured logging
- Router setup in `internal/api/router.go`, entry point in `cmd/<service>/main.go`
- Interface stubs: Use interface + NoOp implementation for external dependencies (k8s, webhooks) to allow incremental implementation
- Handler config structs: Use `FooHandlerConfig` struct for >3 dependencies instead of positional params

---
<!-- Task logs below - APPEND ONLY -->

## Task - db-1
- Created `schema/migrations/001_create_users_table.sql`
- Columns: id (UUID PK), discord_id (TEXT UNIQUE NOT NULL), discord_username (TEXT NOT NULL), avatar_url (TEXT nullable), created_at, updated_at
- Verified against postgres:16-alpine in docker
- **Learnings:** gen_random_uuid() is built into PG 13+, no extension needed

## Task - db-2
- Created `schema/migrations/002_create_minions_table.sql`
- Full lifecycle tracking: status (CHECK constraint), clarification flow, token tracking, cost, PR/error, pod/session, Discord context
- Status constraint uses CHECK vs ENUM for easier future additions
- FK to users(id) with CASCADE delete, indexed for joins
- **Learnings:** CHECK constraint > ENUM for evolving status lists, no migration needed to add values

## Task - db-3
- Created `schema/migrations/003_create_minion_events_table.sql`
- Columns: id (UUID PK), minion_id (FK to minions), timestamp (TIMESTAMPTZ), event_type (TEXT), content (JSONB)
- FK with ON DELETE CASCADE verified, events deleted when parent minion deleted
- Composite index on (minion_id, timestamp) for efficient chronological event retrieval
- **Learnings:** JSONB content with default '{}' allows flexible schema evolution for different event types

## Task - db-4
- Created `schema/migrations/004_create_indexes.sql`
- Indexes: status (filter), (status, last_activity_at) (watchdog), clarification_message_id (reply lookup)
- Used partial index for clarification_message_id (WHERE NOT NULL) to save space
- **Learnings:** Partial indexes exclude NULL values by default, reducing index size for sparse columns

## Task - orchestrator-1
- Created `orchestrator/` Go module with chi router v5
- `cmd/orchestrator/main.go`: entry point, graceful shutdown, slog JSON logging
- `internal/api/router.go`: chi router setup, /health endpoint, request logging middleware
- GET /health returns `{"status":"ok"}` (verified)
- **Learnings:** chi's middleware.NewWrapResponseWriter captures status/bytes for logging; use _ = for ignored encoder errors to satisfy errcheck

## Task - orchestrator-2
- Created `internal/api/middleware.go` with AuthMiddleware
- Uses `crypto/subtle.ConstantTimeCompare` to prevent timing attacks
- Validates `Authorization: Bearer <token>` header format
- /health exempt (defined outside /api route group)
- /api/* routes require valid INTERNAL_API_TOKEN env var
- Added /api/ping placeholder endpoint for testing auth
- **Learnings:** chi middleware only runs for defined routes in a group; need at least one route for middleware to execute

## Task - orchestrator-3
- Created POST /api/minions endpoint with full validation
- Files: `internal/db/db.go` (connection pool), `internal/db/users.go` (user lookup/create), `internal/db/minions.go` (minion CRUD + rate limits), `internal/api/minions.go` (handler)
- User lookup/create: finds by discord_id, creates if missing, updates username if changed (upsert with ON CONFLICT)
- Rate limiting: 10/hr per user (count minions created in last hour), 3 concurrent (count pending/running)
- Repo validation: regex `^[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+(/[a-zA-Z0-9_.-]+)*$` supports nested repos
- Returns 201 with `{id, status, repo, task, created_at}`, 400 for invalid repo, 429 for rate limits
- Updated main.go: requires DATABASE_URL, creates pool, passes to router
- **Learnings:** RouterConfig struct pattern better than many params; pgxpool handles connection pooling automatically

## Task - orchestrator-4
- Implemented GET /api/minions endpoint with status filter and limit
- Files changed: `internal/db/minions.go` (List method), `internal/api/minions.go` (HandleList), `internal/api/router.go` (route wiring)
- Query params: `?status=<status>` filters by status (validated), `?limit=<n>` limits results (default 50, max 200)
- Results ordered by `created_at DESC`
- Returns array of minion summaries: id, status, repo, task, model, pr_url, error, created_at
- **Learnings:** Parameterized query index via helper `itoa` avoids strconv import in db layer; keeping response slim (no full event logs) is the right call for list endpoints

## Task - orchestrator-5
- Implemented GET /api/minions/{id} endpoint with full minion details and recent events
- Created `internal/db/events.go` with EventStore and MinionEvent struct for event retrieval
- Extended MinionHandler to include EventStore dependency
- Response includes: all minion fields, array of 100 most recent events (timestamp DESC)
- Returns 400 for invalid UUID format with INVALID_ID code
- Returns 404 for non-existent minion with NOT_FOUND code
- Files changed: `internal/db/events.go` (new), `internal/api/minions.go`, `internal/api/router.go`, `internal/api/minions_test.go`
- **Learnings:** chi.URLParam requires chi.RouteCtxKey context value in tests, mock via chi.NewRouteContext()

## Task - orchestrator-6
- Implemented DELETE /api/minions/{id} endpoint for minion termination
- Created `internal/k8s/pods.go` with PodTerminator interface and NoOpPodTerminator stub
- Created `internal/webhook/notifier.go` with Notifier interface and NoOpNotifier stub
- Added `Terminate` method to MinionStore using transaction with FOR UPDATE lock (prevents concurrent updates)
- Returns idempotent success for already-terminal minions (completed/failed/terminated)
- Pod termination and webhook notification are best-effort (logged but don't fail request)
- MinionHandler refactored to use config struct pattern for cleaner dependency injection
- Files: `internal/k8s/pods.go` (new), `internal/webhook/notifier.go` (new), `internal/db/minions.go`, `internal/api/minions.go`, `internal/api/router.go`, `cmd/orchestrator/main.go`
- **Learnings:** FOR UPDATE lock in transaction ensures atomic check-and-update; interface stubs allow incremental implementation without blocking dependent tasks

## Task - orchestrator-7
- Implemented POST /api/minions/{id}/callback endpoint for devbox completion callbacks
- Added `Complete` method to `MinionStore` with transaction-based atomic status update (FOR UPDATE lock)
- Handler accepts `{ status, pr_url?, error?, session_id? }` with validation (status must be "completed" or "failed")
- Updates minion with completion data: pr_url, error, session_id, completed_at, last_activity_at
- Notifies Discord bot via webhook (NotifyCompleted or NotifyFailed based on status)
- Returns `{ success: true }` (idempotent: returns success if already terminal)
- Files: `internal/db/minions.go` (Complete method), `internal/api/minions.go` (HandleCallback), `internal/api/router.go`, `internal/api/minions_test.go`
- **Learnings:** Reused same transactional pattern from Terminate for Complete; notification type determined by final status

## Task - orchestrator-8
- Implemented GET /api/stats endpoint for aggregate statistics
- Added `GetStats` method to `MinionStore` with SQL aggregation queries
- Response: `total_cost_usd`, `total_input_tokens`, `total_output_tokens`, `by_model` array
- `by_model` includes: model, cost_usd, input_tokens, output_tokens, count (ordered by cost DESC)
- Uses COALESCE to handle empty tables (returns 0 instead of NULL)
- Files: `internal/db/minions.go` (GetStats, Stats, ModelStats), `internal/api/minions.go` (HandleStats), `internal/api/router.go`
- **Learnings:** COALESCE wrapping SUM aggregates prevents NULL when no rows match; stats endpoint is auth-protected via /api route group

## Task - orchestrator-9
- Already implemented as part of orchestrator-3
- Verified all requirements met: user lookup by discord_id, creation if not exists, FK usage, username storage
- No code changes needed; marked as passed
- **Learnings:** When tasks are tightly coupled (user handling + minion creation), implementing them together avoids artificial separation

## Task - orchestrator-10
- Implemented `PATCH /api/minions/:id/clarification` endpoint
- Added `SetClarification` method to `MinionStore` with transactional status transition (pending → awaiting_clarification)
- Handler validates: question (required), discord_message_id (required), UUID format
- Returns 409 Conflict if minion not in pending status
- Added `ErrInvalidStatusTransition` error type for status transition validation
- Files: `internal/db/minions.go`, `internal/api/minions.go`, `internal/api/router.go`, `internal/api/minions_test.go`
- **Learnings:** Status transitions should be explicit and validated; only allow clarification from pending state to prevent race conditions

## Task - k8s-1
- Implemented pod creation with full security context in `internal/k8s/pods.go`
- Added k8s client-go dependency (v0.35.3)
- PodSpawner interface + Client implementation for real k8s operations
- Security context enforced: runAsNonRoot=true, runAsUser/Group=1000, allowPrivilegeEscalation=false
- Capabilities: DROP ALL
- ReadOnlyRootFilesystem=true with EmptyDir volumes for /workspace, /tmp, /home/minion
- GITHUB_TOKEN and LLM API keys passed via env vars to devbox container
- NoOpPodManager for testing/non-k8s environments
- Files: `internal/k8s/pods.go`, `internal/k8s/pods_test.go`, `go.mod`, `go.sum`
- **Learnings:** client-go uses in-cluster config automatically; EmptyDir volumes needed for writable paths when using read-only root filesystem

## Task - k8s-2
- Implemented `SpawnPodWithRetry` method with exponential backoff
- Added to PodSpawner interface, Client implementation, and NoOpPodManager
- Retry constants: MaxRetries=3, InitialBackoff=1s, MaxBackoff=30s, BackoffMultiplier=2
- Returns `ErrRetriesExhausted` sentinel error when all attempts fail
- Respects context cancellation during backoff waits
- Comprehensive test coverage: success on first attempt, success after retries, exhausted retries, context cancellation
- Files: `internal/k8s/pods.go`, `internal/k8s/pods_test.go`
- **Learnings:** Use `errors.Is()` with sentinel errors for retry exhaustion detection; exponential backoff with cap prevents runaway delays

## Task - k8s-3
- Implemented `WaitForPodReady` method with 5-minute timeout (`PodReadyTimeout`)
- Polls pod status every 2 seconds (`PodPollInterval`)
- On timeout: deletes pod, returns `ErrPodTimeout` sentinel error
- Added `isPodReady` helper that checks for `PodReady` condition with `ConditionTrue`
- Handles unexpected pod termination (Failed/Succeeded phases) with descriptive error
- Updated `PodSpawner` interface, `NoOpPodManager` to include new method
- Files: `internal/k8s/pods.go`, `internal/k8s/pods_test.go`
- **Learnings:** Pod readiness uses the PodReady condition type, not just phase; ticker-based polling with context timeout is idiomatic for k8s watches without informers

## Task - k8s-4
- Created `internal/reconciler/reconciler.go` with Reconciler struct
- Added `ListByStatuses` method to MinionStore for fetching pending/running minions
- Added `ListPods` method to k8s Client and NoOpPodManager for listing minion pods
- Added `MarkFailed` method to MinionStore for marking orphaned minions
- Reconciler runs BEFORE HTTP server in main.go
- Orphan detection: minion with no pod OR pod in Failed/Succeeded phase
- Stray pod detection: pods with minion-id label not in active minions, or no label
- Comprehensive test coverage: 10 test cases covering all scenarios
- Files: `internal/reconciler/reconciler.go` (new), `internal/reconciler/reconciler_test.go` (new), `internal/db/minions.go`, `internal/k8s/pods.go`, `cmd/orchestrator/main.go`
- **Learnings:** Reconciliation should be fail-fast (error on list failures) but graceful on individual item failures (continue processing others)

## Task - github-1
- Created `internal/github/tokens.go` with TokenManager interface and Manager implementation
- Uses `github.com/bradleyfalzon/ghinstallation/v2` for GitHub App authentication
- Manager caches installation transports per installation ID; ghinstallation handles auto-refresh internally (1 min before expiry)
- `GetToken(ctx, repo)` parses "owner/repo", looks up installation via `Apps.FindUserInstallation`, then calls `transport.Token(ctx)`
- 401/403 errors detected via `isAccessRevokedError()` and wrapped with `ErrAccessRevoked` sentinel
- Logging: token refresh failures logged with repo context via slog
- NoOpTokenManager for testing/non-GitHub environments
- Files: `internal/github/tokens.go` (new), `internal/github/tokens_test.go` (new), `go.mod`, `go.sum`
- **Learnings:** ghinstallation handles token caching and refresh automatically; use `Apps.FindUserInstallation` to find installation ID from owner (works for both users and orgs)

## Task - streaming-1
- Created `internal/streaming/sse.go` with SSEClient for connecting to pod /events SSE endpoints
- Implements reconnection with exponential backoff (1s initial, 30s max, 2x multiplier)
- EventHandler interface allows pluggable event handling (persistence, broadcasting)
- Created `internal/streaming/handler.go` with DBEventHandler that persists to minion_events and updates token usage
- Added `InsertEvent` method to EventStore for persisting events
- Added `UpdateTokenUsage` method to MinionStore for atomically incrementing token counters
- Added `GetPodIP` method to k8s Client for resolving pod IP addresses
- Extended PodManager interface to include PodIPProvider
- Token usage extraction: looks for `token_usage`, `usage`, or type=token_usage events
- NoOpEventHandler for testing; mock-based tests for SSE parsing
- Files: `internal/streaming/sse.go` (new), `internal/streaming/handler.go` (new), `internal/streaming/sse_test.go` (new), `internal/db/events.go`, `internal/db/minions.go`, `internal/k8s/pods.go`
- **Learnings:** SSE parsing via bufio.Scanner is simpler than full SSE library; token usage can appear in multiple formats depending on LLM provider

## Task - streaming-2
- Implemented WebSocket endpoint at `GET /api/minions/:id/stream` for live event streaming
- Created `internal/streaming/hub.go` with Hub struct for managing client connections (pub/sub pattern)
- Created `internal/streaming/ws_handler.go` with StreamHandler for WebSocket upgrade handling
- Hub uses channels for register/unregister/broadcast, runs in background goroutine
- Clients grouped by minion ID; broadcasts only go to clients subscribed to that minion
- Added `gorilla/websocket` v1.5.3 dependency
- Updated DBEventHandler to broadcast to Hub after persisting events
- Hub started in main.go before HTTP server, stopped on graceful shutdown
- RouterConfig extended with Hub field; stream endpoint wired in /api route group
- Comprehensive tests: hub lifecycle, multi-client broadcast, minion isolation, graceful disconnect
- Files: `internal/streaming/hub.go` (new), `internal/streaming/ws_handler.go` (new), `internal/streaming/hub_test.go` (new), `internal/streaming/handler.go`, `internal/api/router.go`, `cmd/orchestrator/main.go`, `go.mod`, `go.sum`
- **Learnings:** gorilla/websocket needs ReadPump to handle pongs and WritePump for sends; buffered broadcast channel prevents SSE handler blocking

## Task - watchdog-1
- Created `internal/watchdog/watchdog.go` with Watchdog struct for background monitoring
- Background goroutine runs every 5 minutes (CheckInterval constant)
- Queries running minions with last_activity_at > 30min ago via ListIdleRunning method
- POSTs idle alerts to Discord bot webhook for each idle minion
- Checks for pods in Failed phase (OOMKilled, Evicted, etc.) via ListPods
- Marks corresponding minion as failed with "Pod terminated: Failed" error message
- Added `ListIdleRunning` method to MinionStore using interval-based SQL query
- Watchdog integrated into main.go: starts as goroutine before HTTP server, stopped on graceful shutdown
- Comprehensive test suite: 12 tests covering idle detection, failed pods, error handling, run/stop lifecycle
- Files: `internal/watchdog/watchdog.go` (new), `internal/watchdog/watchdog_test.go` (new), `internal/db/minions.go`, `cmd/orchestrator/main.go`
- **Learnings:** Use interface-based dependencies (MinionQuerier, PodStatusChecker) for testability; watchdog can use existing k8s.PodManager.ListPods for pod status checks

## Task - watchdog-2
- Implemented clarification timeout enforcement in `internal/watchdog/watchdog.go`
- Added `ListClarificationTimeouts` method to `MinionStore` (queries awaiting_clarification + created_at > timeout)
- Added `ClarificationTimeout` constant (24h) and `ClarificationCheckInterval` (10min)
- Watchdog runs clarification check every 10 min (piggybacks on existing 5-min ticker, skips every other run)
- Timed-out minions: marked as failed with "Clarification timeout" error, notified via `NotifyClarificationTimeout` webhook
- Extended `MinionQuerier` interface with `ListClarificationTimeouts` method
- Added 4 test cases: basic timeout, skipped-if-too-soon, query error, multiple timeouts
- Files: `internal/watchdog/watchdog.go`, `internal/watchdog/watchdog_test.go`, `internal/db/minions.go`
- **Learnings:** Piggyback on existing ticker with skip logic is simpler than separate timers; the clarification timeout uses created_at (not last_activity_at) since minion never became "running"

## Task - dedup-1
- Implemented duplicate prevention using `pg_advisory_xact_lock` for race-condition-safe duplicate detection
- Added `CreateOrFindDuplicate` method to `MinionStore` that atomically checks for duplicates and creates
- Hash function `repoTaskHash` generates 64-bit key from SHA-256 of repo+task for advisory lock
- `DuplicateWindow` constant set to 5 minutes as per PRD
- Returns existing minion (with `WasDuplicate: true`) if same repo+task created within window
- Handler returns HTTP 200 with `duplicate: true` for existing minions, HTTP 201 for new ones
- Added `TODO: Add --force flag support` comment for future enhancement
- Files: `internal/db/minions.go`, `internal/db/minions_test.go`, `internal/api/minions.go`
- **Learnings:** `pg_advisory_xact_lock` (vs `pg_advisory_lock`) is transaction-scoped, auto-releases on commit/rollback; SHA-256 truncated to int64 provides sufficient collision resistance for advisory locks

## Task - shutdown-1
- Verified graceful shutdown already implemented in `cmd/orchestrator/main.go`
- SIGTERM/SIGINT handlers: lines 127-129 via `signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)`
- Stops accepting new requests: `srv.Shutdown()` call at line 144
- 30s timeout for in-flight requests: `context.WithTimeout(context.Background(), 30*time.Second)` at lines 141-142
- Database pool closed: `defer pool.Close()` at line 61 (runs after Shutdown returns)
- Running minions continue in pods: no pod termination logic on shutdown (pods are independent)
- Also stops watchdog and WebSocket hub gracefully before HTTP server shutdown
- No code changes needed; marked as passed
- **Learnings:** Graceful shutdown was built incrementally as orchestrator features were added (hub, watchdog); important to stop background goroutines BEFORE HTTP server to prevent race conditions

## Task - devbox-1
- Created `devbox/Dockerfile` based on golang:1.22-bookworm
- Includes: git, curl, jq, gh CLI (via GitHub's apt repo), OpenCode binary
- OpenCode installed via GitHub releases API with arch detection (amd64→x64, arm64→arm64)
- Non-root user: minion:minion with UID/GID 1000
- WORKDIR set to /workspace, owned by minion user
- Verification step confirms all tools are installed and accessible
- Files: `devbox/Dockerfile` (new)
- **Learnings:** OpenCode asset naming uses x64 not amd64; exact jq match (`select(.name == $name)`) is more reliable than contains() for asset selection

## Task - devbox-2
- Created `devbox/entrypoint.sh` for minion task execution
- Clones repo using GITHUB_TOKEN via git credential helper (safer than embedding in URL)
- Starts `opencode serve` on port 4096 (configurable via OPENCODE_PORT)
- Waits for `/global/health` endpoint readiness with 60s timeout
- Creates session via POST /session, sends task via POST /session/:id/prompt_async
- Uses jq for all JSON construction, preventing shell injection from task content
- Polls /session/status for completion detection (idle = done)
- Updated Dockerfile to COPY entrypoint.sh and set as ENTRYPOINT
- Files: `devbox/entrypoint.sh` (new), `devbox/Dockerfile` (updated)
- **Learnings:** OpenCode's prompt_async is non-blocking (returns 204 immediately), use /session/status polling to detect completion; git credential helper is cleaner than URL-embedded tokens

## Task - devbox-3
- Implemented full PR creation flow in `devbox/entrypoint.sh`
- `has_changes()` function checks `git status --porcelain` for any modifications
- `create_pr()` creates branch `minion/<uuid>`, stages all changes, commits with task-based message
- Git user config: `minion@anomaly.co` / `Minion`
- `git push -u origin` pushes branch, `gh pr create` creates PR (uses GITHUB_TOKEN automatically)
- `send_callback()` POSTs to orchestrator with status, pr_url, error, session_id
- Updated k8s `SpawnParams` to use `OrchestratorURL` + `InternalAPIToken` instead of `CallbackURL`
- Added `INTERNAL_API_TOKEN` to required env vars in entrypoint.sh
- Files: `devbox/entrypoint.sh`, `orchestrator/internal/k8s/pods.go`, `orchestrator/internal/k8s/pods_test.go`
- **Learnings:** gh CLI uses GITHUB_TOKEN env var automatically for auth; jq's `--arg` is great for safe JSON construction without shell escaping issues

## Task - devbox-4
- Implemented callback retry with exponential backoff in `devbox/entrypoint.sh`
- Constants: `CALLBACK_MAX_RETRIES=5`, `CALLBACK_INITIAL_BACKOFF=1`, `CALLBACK_MAX_BACKOFF=60`
- Backoff doubles each retry (1s → 2s → 4s → 8s → 16s), capped at 60s
- Curl captures HTTP status reliably, falls back to "000" on network failure
- All `send_callback` calls in `main()` now check return value, exit 1 on failure
- Non-zero exit ensures k8s marks pod as Failed, visible in orchestrator reconciliation
- Files: `devbox/entrypoint.sh`
- **Learnings:** Use `http_code=$(curl ... || http_code="000"` pattern to handle both curl errors and HTTP status; main function must explicitly check all callback returns since set -e doesn't apply to command substitutions

## Task - devbox-5
- Implemented PR edge case handling in `devbox/entrypoint.sh`
- Exit codes: PR_SUCCESS=0, PR_NO_CHANGES=10, PR_PUSH_CONFLICT=11, PR_ERROR=1
- Existing PR detection: checks stderr for "already exists", fetches URL via `gh pr view`
- Push conflict detection: grep for rejected/non-fast-forward/conflict in push output
- gh CLI stderr: captured via process substitution `2> >(tee ...)`, always logged
- No changes: sends `result_type: "no_changes"` with completed status (not failure)
- Files: `devbox/entrypoint.sh`
- **Learnings:** Use process substitution `2> >(cmd)` to capture stderr while still showing it; `gh pr view --json url -q '.url'` reliably fetches existing PR URL
