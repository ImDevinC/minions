# Progress Log
PRD: minion-observability-fix
Started: 2026-03-22

## Codebase Patterns
- **Database column additions**: Add field to Minion struct, update all query SELECT/Scan statements (8 methods in this case), add accessor methods
- **Idempotent operations**: StorePassword and ClearPassword are both idempotent - safe to call multiple times

---
<!-- Task logs below - APPEND ONLY -->

## Task - db-1
- Created migration schema/migrations/005_add_opencode_password.sql
- Added OpencodePassword string field to Minion struct in orchestrator/internal/db/minions.go:57
- Implemented StorePassword() and ClearPassword() methods in orchestrator/internal/db/minions.go:1044-1060
- Updated all 8 query methods to SELECT and Scan opencode_password:
  - GetByID (line 264)
  - FindRecentDuplicate (line 156)
  - List (line 364)
  - ListByStatuses (line 668)
  - ListPending (line 715)
  - ListIdleRunning (line 794)
  - ListClarificationTimeouts (line 837)
  - GetByClarificationMessageID (line 881)
- Added unit test TestPasswordMethods in orchestrator/internal/db/minions_test.go
- All orchestrator tests pass
- **Learnings**: Column is TEXT NULL to allow cleanup on completion; OpencodePassword field is non-pointer string (empty string when NULL)

## Task - research-1
- Located callback handler: HandleCallback in orchestrator/internal/api/minions.go:490-579
  - Handles completed and failed statuses via h.minions.Complete()
  - Already idempotent (returns early if minion already terminal)
  - Will need to add ClearPassword() after line 533 (after Complete succeeds)
- Located watchdog failure paths:
  - checkFailedPods (line 206): calls w.minions.MarkFailed() for pod failures (OOMKilled, Evicted)
  - checkClarificationTimeouts (line 238): calls w.minions.MarkFailed() for timeout
- Located termination handler: HandleDelete in orchestrator/internal/api/minions.go:414-473
  - Handles user-initiated deletion via h.minions.Terminate()
  - Will need to add ClearPassword() after line 432 (after Terminate succeeds)
- **Learnings**: Three terminal state entry points - callback (completed/failed), watchdog (MarkFailed), and delete (terminated). MarkFailed is called from two watchdog paths.

## Task - orchestrator-interfaces-1
- Updated SSEConnector interface in orchestrator/internal/spawner/spawner.go:56
  - Changed Connect signature to accept password parameter: Connect(ctx, minionID, podName, password string)
- Updated SSEClient implementation in orchestrator/internal/streaming/sse.go:
  - Connect method (line 107): accepts password parameter
  - connectWithRetry method (line 133): accepts and threads password parameter
  - streamEvents method (line 183): accepts password parameter (not yet used for auth - deferred to orchestrator-core-1)
- Updated spawner.go processMinion call site (line 275): passes empty string for now (TODO comment added for spawner-1)
- Updated test mocks:
  - mockSSEConnector in spawner_test.go: updated Connect signature and struct fields
  - SSE test call sites updated to pass empty string
- All orchestrator tests pass
- **Learnings**: Interface changes must be applied atomically across interface definition, implementation, mock, and call sites to maintain type safety

