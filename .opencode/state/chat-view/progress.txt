# Progress Log
PRD: chat-view
Started: 2026-03-23

## Codebase Patterns
<!-- Consolidate reusable patterns here -->
- Event content structure: messageID/part info can be at root, in `properties.part`, or in `part` directly - check all locations
- Use `Array.from(map.values())` instead of iterating `map.values()` directly to avoid TS downlevelIteration issues
- Delta tracking: use `DeltaState` with both `textByPart` map and `processedEventIds` set to avoid double-counting on re-aggregation
- Subtask linking: task tool `callID` becomes the subtask's sessionID; filter events by sessionID to group subtask conversations

---
<!-- Task logs below - APPEND ONLY -->

## Task - setup-1
- Added react-markdown@^9.1.0 and remark-gfm@^4.0.1 to control-panel
- Files changed: control-panel/package.json, control-panel/package-lock.json
- **Learnings:** .next/types can hold stale references to deleted routes; `rm -rf .next` clears this

## Task - types-1
- Defined ChatMessage, ToolCall, ToolCallStatus, and SystemMessage types
- Files changed: control-panel/src/types/minion.ts (lines 58-106)
- **Learnings:** None - straightforward type definitions following PRD spec

## Task - aggregation-1
- Implemented event aggregation logic in control-panel/src/lib/event-aggregation.ts
- Groups events by messageID into ChatMessages
- System events (no messageID, agent/retry parts, session.error) become SystemMessages
- Deduplicates by event ID, sorts by timestamp, filters heartbeats
- Handles message.updated events for metadata updates
- Files changed: control-panel/src/lib/event-aggregation.ts (new)
- **Learnings:** Event content structure varies - messageID can be at root, in properties.part, or part directly

## Task - aggregation-2
- Implemented delta accumulation for streaming text
- Added `DeltaState` interface with `textByPart` Map and `processedEventIds` Set
- Delta events (`properties.delta: true`) append to buffer, non-delta replace
- Track processed event IDs to avoid double-counting on re-aggregation
- Added vitest for testing, wrote unit tests for delta/replacement behavior
- Files changed: control-panel/src/lib/event-aggregation.ts, control-panel/src/lib/event-aggregation.test.ts (new), control-panel/package.json, control-panel/vitest.config.ts (new)
- **Learnings:** Persistent state across aggregation calls requires tracking both accumulated text AND which events have been processed

## Task - aggregation-3
- Implemented subtask event aggregation for nested message threads
- Added `SubtaskThread` type with sessionID, description, agent, and nested messages array
- Updated `ChatMessage` to include `subtasks: SubtaskThread[]` field
- Added `getSessionID()` helper to extract session ID from events
- Added `extractSubtaskInfo()` to identify task tool calls and their callIDs
- Modified `aggregateEvents()` to: detect root session ID, collect subtask session IDs from task tools, filter events by session, recursively aggregate subtask events
- Added 5 unit tests covering session ID extraction, subtask grouping, recursive aggregation, and multi-subtask separation
- Files changed: control-panel/src/types/minion.ts (lines 78-93), control-panel/src/lib/event-aggregation.ts, control-panel/src/lib/event-aggregation.test.ts
- **Learnings:** Task tool `callID` becomes subtask session ID; events filtered by matching sessionID; use separate deltaState per subtask to avoid pollution

## Task - component-4b
- Added smooth CSS collapse animation to ToolCallCard expand/collapse
- Accordion behavior already worked (ChatView tracks single expandedToolId globally)
- Used `grid-template-rows` transition for smooth height animation (grid-rows-[0fr] -> grid-rows-[1fr])
- Added opacity transition for content fade effect
- Files changed: control-panel/src/components/tool-call-card.tsx
- **Learnings:** `grid-template-rows` with `0fr`/`1fr` is the modern CSS way to animate height from 0 to auto smoothly; requires overflow-hidden wrapper

## Task - component-1
- Implemented ChatView container component with virtualized list
- Takes MinionEvent[] prop, aggregates via aggregateEvents() into ChatMessage/SystemMessage
- Uses @tanstack/react-virtual with overscan: 20 for smooth scrolling
- Maintains persistent DeltaState ref across renders for streaming accumulation
- Combines chat messages and system messages into chronological RenderItem[] union
- Includes placeholder ChatMessageRow and SystemMessageRow (to be replaced in future tasks)
- Files changed: control-panel/src/components/chat-view.tsx (new)
- **Learnings:** Use useRef for DeltaState to persist across renders without triggering re-computation; estimateSize can vary by item type for better virtualization hints

## Task - component-2
- Implemented ChatMessageRow with react-markdown and react-syntax-highlighter
- Text renders via ReactMarkdown with remarkGfm plugin for GitHub-flavored markdown
- Code blocks use SyntaxHighlighter with oneDark theme, horizontal scroll (no wrap)
- Memoized markdown parsing with useMemo keyed on text content
- Empty/null content returns null (no blank bubbles)
- Files changed: control-panel/src/components/chat-message-row.tsx (new), control-panel/src/components/chat-view.tsx (import new component)
- **Learnings:** react-syntax-highlighter needs wrapLines={false} and wrapLongLines={false} plus whiteSpace: "pre" for horizontal scroll

## Task - component-3
- Implemented ThinkingBlock component for collapsible reasoning content
- Default state collapsed showing "▶ Thinking..." with muted gray-500 text
- Click toggles expansion, expanded state shows full reasoning with gray-400 text
- Collapsed uses smaller text (text-xs), expanded uses text-sm
- Smooth rotation transition on arrow indicator
- Replaced placeholder in ChatMessageRow with ThinkingBlock component
- Files changed: control-panel/src/components/thinking-block.tsx (new), control-panel/src/components/chat-message-row.tsx (import + use ThinkingBlock)
- **Learnings:** useState(false) for default collapsed; rotate-90 class for arrow animation

## Task - component-4a
- Implemented ToolCallCard component for compact expandable tool invocation cards
- Tool icon mapping: read=📖, write/edit=✏️, glob=🔍, grep=🔎, bash=⚙️, task=🤖, webfetch=🌐, fallback=🔧
- Status badge colors: pending (gray-600), running (blue-600 animate-pulse), completed (green-700), error (red-700)
- Expanded state shows syntax-highlighted JSON input with oneDark theme
- Output/error sections render when present
- Integrated into ChatMessageRow with expandedToolId state managed by ChatView
- Files changed: control-panel/src/components/tool-call-card.tsx (new), control-panel/src/components/chat-message-row.tsx, control-panel/src/components/chat-view.tsx
- **Learnings:** Accordion state across messages uses "messageId:toolId" format in parent ChatView; basic expansion works but accordion auto-collapse behavior is task 4b

## Task - component-4c
- Implemented truncation for large inputs/outputs (>10KB)
- Added `TruncatedContent` component for plain text with expand/collapse button
- Added `TruncatedSyntaxHighlighter` component for syntax-highlighted JSON with expand/collapse
- Shows first ~500 chars with "..." indicator when truncated
- "Show full input/output" button toggles full content display
- Files changed: control-panel/src/components/tool-call-card.tsx
- **Learnings:** Separate components for plain text vs syntax-highlighted truncation keeps concerns clean; useState per-component handles individual expand state
